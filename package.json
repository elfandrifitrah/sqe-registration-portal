{
  "name": "registration-recruitment-workspace-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "SQE Case Study Task 7 — running prototype of the Registration & Recruitment Workspace (reviewer screening workspace + candidate comparison dashboard)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:pages": "tsc --noEmit && vite build --base=/sqe-registration-portal/",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  }
}
